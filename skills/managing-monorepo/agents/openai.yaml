interface:
  display_name: "Managing Monorepo"
  short_description: "TypeScript workspace imports and canonical package scripts"
  default_prompt: "Use $managing-monorepo when adding a package, choosing import style, or naming a script other packages must run."

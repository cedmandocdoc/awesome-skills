interface:
  display_name: "Managing Context Sessions"
  short_description: "Create, continue, update, and close context sessions that carry focused context across agent sessions"
  default_prompt: "Use $managing-context-sessions to create, continue, update, or close a context session under sessions/."
policy:
  allow_implicit_invocation: false
